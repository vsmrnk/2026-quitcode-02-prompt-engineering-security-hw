# Патерни для локальної count-only верифікації санітизації (grep -Ei -f).
# Це config-файл сканера (як gitleaks rules), а не документ із даними:
# token-подібні рядки тут — навмисно, бо ми їх ШУКАЄМО.
# Набір узгоджено з docs/sanitization-checklist.md §2.
# Використання: grep -cEi -f docs/secret-patterns.txt <файл>   # має бути 0
fake_
xox[baprs]-
sk-
pat[a-z0-9]{6,}
app[a-z0-9]{8,}
akia[0-9a-z]{16}
-----begin
postgres://
mysql://
mongodb://
redis://
amqp://
[a-z0-9._%+-]+@[a-z0-9.-]+\.[a-z]{2,}
\+[0-9][0-9() -]{7,}
\([0-9]{2,4}\)[ -]?[0-9]{2,}
