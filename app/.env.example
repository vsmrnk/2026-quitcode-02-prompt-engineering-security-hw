# Контракт змінних середовища (out-of-band секрети).
# Реальні значення НІКОЛИ не комітяться: справжній .env у .gitignore,
# а сюди йдуть лише ІМЕНА змінних. Див. docs/sanitized-brief.md і
# docs/sanitization-checklist.md §4 (креденшели — тільки через env).

# Демо-ключ для Task C (фейковий, нічого не відкриває)
DEMO_API_KEY=sk-fake-do-not-use-0000

# Інтеграції з sanitized-brief.md (значення видаються лише на запуску)
CRM_API_KEY=
CHAT_BOT_TOKEN=
TABLE_PAT=
TABLE_BASE_ID=
INTAKE_WEBHOOK_URL=
ORDERS_DB_URL=
